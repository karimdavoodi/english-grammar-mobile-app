# content/tracks

Authored tracks as pure data. Each track is assembled from small cluster modules
so content can be authored and reviewed in parallel without changing the loader
or gameplay code. The Basic track currently has `basic-01` and `basic-02`
clusters; future tracks follow the same structure.
